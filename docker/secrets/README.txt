# Put signing keys here (gitignored):
#   sui.key   - Sui suiprivkey (Bech32) that signs settlement transactions
#   block.key - block signing key
# These are mounted read-only into the podseq container at /secrets.
